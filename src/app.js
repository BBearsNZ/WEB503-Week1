//adding a delete button to a call back function:

// deleteBtn.addEventListener('click', e => {
//let id - e.target.parentElement.parentElement.getAttribute('data-id');
//db.collection('todo').doc(id).delete();
// });